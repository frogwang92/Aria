INCLUDE_DIRECTORIES(${COMMON})
INCLUDE_DIRECTORIES(${PYTHON27})

SET(DATA_SRC
        Symbol.cpp Symbol.h
        RealTimeDataFeed.cpp RealTimeDataFeed.h)

ADD_LIBRARY(Data SHARED ${DATA_SRC})
TARGET_LINK_LIBRARIES(Data FIXClient)