INCLUDE_DIRECTORIES(${COMMON})

SET(FIXCLIENT_SRC FIXClient.cpp FIXClient.h FIXClientHandler.cpp FIXClientHandler.h)

ADD_LIBRARY(FIXClient SHARED ${FIXCLIENT_SRC})
TARGET_LINK_LIBRARIES(FIXClient ${QUICKFIX_LIB})